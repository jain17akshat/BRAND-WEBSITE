# ─── API Configuration ──────────────────────────────────────
# Deployed Express backend base URL (ending in /api).
# Leave blank during local development to use the Vite proxy (/api -> http://localhost:4000).
VITE_API_URL=http://localhost:4000/api
